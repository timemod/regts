build-job:
  stage: build
  script:
    - export R_LIBS_USER=$HOME/R/cpb_r_packages/4.2
    - R -e "print(.libPaths())"
    - make install

test-job1:
  stage: test
  script:
    - echo "This job tests something"
    - R CMD check pkg


deploy-prod:
  stage: deploy
  script:
    - echo "This job deploys something from the $CI_COMMIT_BRANCH branch."
  environment: production
